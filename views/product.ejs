<%- include('partials/header.ejs') %>

<!-- product detail -->
<% for (let i=0; i < productos.length; i++) { %>
    <% if (productos[i].id == id) { %>
        <div class="container products-wrapper">
            <div class="d-flex align-items-center">
                <h2 class="products-title">
                    <%= productos[i].nombre %>
                </h2>
                <p class="product-detail-description">
                    <a href="">Editar</a>
                </p>
            </div>
            <div class="product-detail">
                <div class="row">
                    <article class="col-12 col-lg-8 product-detail-info">
                        <img src="<%=productos[i].foto %>" alt="" class="product-detail-img">
                        <p class="product-detail-description">
                            <%= productos[i].descripcion %>
                        </p>
                    </article>
                    <div class="col-12 col-lg-4">
                        <% for (let r=0; r < productos[i].comentarios.length; r++) { %>
                            <section class="comments">
                                <article>
                                    <img src="<%=productos[i].comentarios[r].perfil %>" alt="">
                                    <% if (productos[i].comentarios[r].usuario !== undefined) { %>
                                        <div class="data">
                                            <p class="user">
                                                <strong> <a href="/profile"> <%= productos[i].comentarios[r].usuario %></a> </strong>
                                                <%= productos[i].comentarios[r].texto %>
                                            </p>
                                        </div>
                                    <% } %>
									
                                </article>
                            </section>
                        <% } %>
						<form class="form" action="" method="GET">
							<input class="form-input" type="text" value="" placeholder="Agregar comentario">
							<button type="submit" class="buy-now-button">AGREGAR COMENTARIO</button>
						</form>
						<hr>
                    </div>
                </div>
            </div>
        </div>
    <% } %>
<% } %>


<%- include('partials/footer.ejs') %>
